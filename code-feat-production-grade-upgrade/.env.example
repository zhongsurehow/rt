# 这是一个环境变量模板文件。
# 请将其复制为 .env 文件 (cp .env.example .env) 并根据您的需求进行修改。
# This is a template for environment variables.
# Copy this file to .env (cp .env.example .env) and modify it as needed.

# --- 数据库配置 (可选) ---
# 用于存储实时行情数据的PostgreSQL/TimescaleDB数据库连接字符串。
# 如果留空，依赖数据库的功能（如历史分析标签页）将被禁用。
# Database DSN for storing real-time ticker data.
# If left empty, database-dependent features will be disabled.
# Example: DB_DSN="postgresql+asyncpg://user:password@host:port/dbname"
DB_DSN=

# --- CEX API 密钥 (可选) ---
# 大部分公开数据（如行情、K线）不需要API密钥。
# 密钥主要用于获取账户相关信息，例如精确的转账费用。
# Most public data (tickers, k-lines) does not require API keys.
# Keys are primarily for account-specific information like transfer fees.
BINANCE_API_KEY=
BINANCE_API_SECRET=
OKX_API_KEY=
OKX_API_SECRET=
BYBIT_API_KEY=
BYBIT_API_SECRET=

# --- DEX 提供商配置 (可选) ---
# 用于从Uniswap V3等去中心化交易所获取数据的以太坊RPC节点URL。
# Ethereum RPC Node URL for fetching data from DEXes like Uniswap V3.
RPC_URL_ETHEREUM="https://eth.llamarpc.com"
